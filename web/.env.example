# Leave unset for local and preview builds so they remain non-indexable.
# Set only on the approved production deployment, with no trailing path.
# NEXT_PUBLIC_SITE_URL=https://your-project.vercel.app
